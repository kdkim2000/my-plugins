---
name: blank-template
description: 새로운 스킬을 만들 때 복사해 사용하는 빈 템플릿입니다. 실제 작업에는 자동으로 사용되지 않습니다. (Internal template — do not auto-trigger; copy this folder and rewrite name/description before use.)
---

# (TODO) 스킬 이름

> 이 파일은 새 스킬을 만들 때 복사해 쓰는 골격입니다.
> 사용 절차:
> 1. `skills/blank-template` 폴더를 통째로 복사하여 새 폴더 이름(kebab-case)으로 변경합니다.
> 2. 위 frontmatter의 `name`과 `description`을 새 스킬에 맞게 다시 씁니다.
>    - `name`은 폴더명과 동일하게.
>    - `description`은 **언제 이 스킬이 트리거되어야 하는지** 트리거 키워드를 포함해 한국어로 명확히 작성합니다.
> 3. 아래 본문 섹션의 `(TODO)` 표시를 모두 채웁니다.
> 4. 작업이 끝나면 이 인용 블록(`>` 부분)을 통째로 삭제합니다.

## 개요

(TODO) 이 스킬이 무엇을 하는지 1~2문장으로 설명합니다.

## 규칙

1. (TODO) 출력 길이/형식 제한 (예: 5줄 이내 등)
2. (TODO) 톤 / 말투 가이드 (예: 친근하게 / 중립적으로)
3. (TODO) 반드시 지켜야 할 제약 (금지 사항 등)

## 출력 형식

(TODO) 사용자가 받게 될 결과물의 형태를 한두 줄로 보여줍니다. 실제 응답에서는 가능한 한 코드펜스 없이 그대로 출력하도록 안내합니다.

## 엣지 케이스

- (TODO) 입력이 비어 있거나 지나치게 짧을 때
- (TODO) 입력이 모호할 때
- (TODO) 민감 정보가 포함되어 있을 때
- (TODO) 스킬의 적용 범위를 벗어난 요청이 들어올 때

## 예시

**입력:** (TODO) 자연스러운 사용자 발화 예시
**출력:**
(TODO) 위 규칙과 출력 형식을 따른 결과 예시

**입력:** (TODO) 다른 톤/길이의 예시
**출력:**
(TODO) 결과 예시
